<Page
    x:Class="Kikaria.Views.MarkdownEditorPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Kikaria.Controls"
    mc:Ignorable="d">

    <Grid x:Name="RootGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Padding="16,12,16,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Button x:Name="BackButton"
                    Grid.Column="0"
                    Content="&#xE72B;"
                    FontFamily="Segoe MDL2 Assets"
                    FontSize="16"
                    Background="Transparent"
                    Click="BackButton_Click" />

            <TextBlock Grid.Column="1"
                       x:Name="PageTitle"
                       Text="知识点上传"
                       FontSize="20"
                       FontWeight="SemiBold"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       FontFamily="Microsoft YaHei, PingFang SC" />

            <Button x:Name="ApplyButton"
                    Grid.Column="2"
                    Content="应用"
                    Height="36"
                    CornerRadius="12"
                    FontSize="14"
                    FontWeight="SemiBold"
                    Click="ApplyButton_Click"
                    FontFamily="Microsoft YaHei, PingFang SC" />
        </Grid>

        <Grid Grid.Row="1" Padding="24,16,24,24">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <controls:LiquidGlassCard x:Name="EditorCard"
                                      Grid.Row="0"
                                      CardCornerRadius="18"
                                      Padding="16,12,16,12">
                <TextBox x:Name="MarkdownTextBox"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         FontFamily="Cascadia Code, Consolas"
                         FontSize="14"
                         ScrollViewer.VerticalScrollBarVisibility="Auto"
                         ScrollViewer.HorizontalScrollBarVisibility="Auto"
                         BorderThickness="0"
                         Background="Transparent"
                         PlaceholderText="在此输入或粘贴 Markdown 格式的知识点内容...&#x0a;&#x0a;# 知识点标题&#x0a;&#x0a;tags: tag1, tag2&#x0a;&#x0a;hint:&#x0a;提示内容&#x0a;&#x0a;content:&#x0a;答案内容&#x0a;&#x0a;---" />
            </controls:LiquidGlassCard>

            <InfoBar x:Name="ErrorBar"
                     Grid.Row="1"
                     IsOpen="False"
                     Severity="Error"
                     Margin="0,8,0,0"
                     FontFamily="Microsoft YaHei, PingFang SC" />
        </Grid>
    </Grid>
</Page>
