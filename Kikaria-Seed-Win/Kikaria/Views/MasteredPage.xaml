<Page
    x:Class="Kikaria.Views.MasteredPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d">

    <Grid Background="{Binding Source={StaticResource KikariaTheme}, Path=PageGradient}">
        <Grid Padding="40" MaxWidth="800" HorizontalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="20" VerticalAlignment="Center" Margin="0,0,0,40">
                <Button Style="{StaticResource LiquidGlassButton}" Content="← Back" Click="BackButton_Click" />
                <TextBlock Text="Mastered Knowledge Points" Style="{StaticResource CardTitleText}" FontSize="32" />
            </StackPanel>

            <ListView Grid.Row="1" x:Name="MasteredListView" SelectionMode="None">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="local:KnowledgePoint">
                        <Border Style="{StaticResource LiquidGlassCard}" Margin="0,10">
                            <StackPanel Spacing="20">
                                <TextBlock Text="{Binding Title}" Style="{StaticResource CardTitleText}" />
                                
                                <Border Background="{StaticResource KikariaTheme.Mist}" Padding="15" CornerRadius="8">
                                    <StackPanel Spacing="5">
                                        <TextBlock Text="Hint:" FontWeight="SemiBold" Foreground="{StaticResource KikariaTheme.SoftText}" />
                                        <TextBlock Text="{Binding Hint}" TextWrapping="Wrap" />
                                    </StackPanel>
                                </Border>

                                <Border Background="{StaticResource KikariaTheme.MasteredCompletedGreen}" Padding="15" CornerRadius="8">
                                    <StackPanel Spacing="5">
                                        <TextBlock Text="Content:" FontWeight="SemiBold" Foreground="{StaticResource KikariaTheme.MasteredDeepGreen}" />
                                        <TextBlock Text="{Binding Content}" TextWrapping="Wrap" Foreground="{StaticResource KikariaTheme.MasteredDeepGreen}" />
                                    </StackPanel>
                                </Border>

                                <Button Content="Remove from Mastered" 
                                        Tag="{Binding Id}" 
                                        Click="RemoveFromMasteredButton_Click"
                                        Style="{StaticResource LiquidGlassButton}"
                                        Background="{Binding Source={StaticResource KikariaTheme}, Path=RemoveGradient}"
                                        HorizontalAlignment="Right" />
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
                <ListView.EmptyContent>
                    <TextBlock Text="No mastered knowledge points yet." HorizontalAlignment="Center" Margin="0,100,0,0" Style="{StaticResource CardSubtitleText}" />
                </ListView.EmptyContent>
            </ListView>
        </Grid>
    </Grid>
</Page>