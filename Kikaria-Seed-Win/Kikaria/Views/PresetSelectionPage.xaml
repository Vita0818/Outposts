<Page
    x:Class="Kikaria.Views.PresetSelectionPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d">

    <Grid Background="LightGray">
        <Grid Padding="40" MaxWidth="800" HorizontalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="20" VerticalAlignment="Center" Margin="0,0,0,40">
                <Button Style="{StaticResource LiquidGlassButton}" Content="← Back" Click="BackButton_Click" />
                <TextBlock Text="Select Preset" Style="{StaticResource CardTitleText}" FontSize="32" />
            </StackPanel>

            <ListView Grid.Row="1" x:Name="PresetsListView" SelectionMode="Single" SelectionChanged="PresetsListView_SelectionChanged">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource LiquidGlassCard}" Margin="0,10">
                            <StackPanel Spacing="8">
                                <TextBlock Text="{Binding Name}" Style="{StaticResource CardTitleText}" />
                                <TextBlock Text="{Binding Subtitle}" Style="{StaticResource CardSubtitleText}" />
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
                <ListView.EmptyContent>
                    <TextBlock Text="No presets available." HorizontalAlignment="Center" Margin="0,100,0,0" Style="{StaticResource CardSubtitleText}" />
                </ListView.EmptyContent>
            </ListView>
        </Grid>
    </Grid>
</Page>
