BEGIN_PROMPT: Do not execute until you receive END_PROMPT.

[ROUND 3 / BATCH outposts-ui-structure-build-recovery-winui-native-round]

Mandatory preflight:
1. MODEL_CHECK_RESULT: current model must be DeepSeek V4 Pro / deepseek-v4-pro / deepseek-v4-pro[1m]. If not, stop.
2. PATH_CHECK_RESULT: current working directory must be /Users/vita/Vitemis/Outposts/Rokurics-Windows. If not, stop.
3. SOURCE_READONLY_CHECK: Apple source and Rokurics-macOS-Ref are read-only. Writes stay inside /Users/vita/Vitemis/Outposts/Rokurics-Windows or .outposts-supervisor evidence/report paths.

Context:
- Round 1 called qwen-vision on macOS reference screenshots and fixed SymbolIcon.FontSize by using FontIcon.
- Round 2 added MicaBackdrop, code-behind titlebar extension, and RequestedTheme=Dark.
- WMC0011 remaining was reported as 0.
- Windows host validation is still pending and must not be faked.
- Next recommendation: replace custom ListView sidebar with native NavigationView if feasible.

Round 3 task:
- Make a focused WinUI 3 / Windows 11 native structural improvement around navigation.
- Prefer replacing the custom sidebar/ListView shell with NavigationView only if this can be done narrowly and safely without architecture rewrite.
- If a full NavigationView migration is too large, make a smaller preparatory change that clearly moves toward native NavigationView and report the exact remaining steps.
- Preserve existing pages and routing behavior.
- Do not change framework, do not switch to WPF/Avalonia/MAUI/Electron/WebView/Flutter.
- Continue static XAML compatibility scan after changes.
- Do not claim Debug/ARM64 build/window launch on macOS; report WINDOWS_HOST_VALIDATION_PENDING unless there is real Win11 ARM + VS2022 evidence.

Forbidden:
- Do not modify Apple source or reference image directories.
- Do not read/send secrets, .env, API keys, tokens, certificates, p12, ssh keys, Keychain contents.
- Do not git clean/reset/restore/checkout. Do not commit/push/PR. Do not delete visual evidence.

Final report fields:
MODEL_CHECK_RESULT
PATH_CHECK_RESULT
SOURCE_READONLY_CHECK
PROJECT_NAME
ROUND_INDEX
QWEN_REQUIRED
QWEN_CALLED
QWEN_VALID_VISUAL_EVIDENCE
QWEN_COMPARE_SCREENSHOTS_COMPLETED
REFERENCE_SCREENSHOTS_USED
ACTUAL_SCREENSHOTS
VISION_TOOLS_CALLED
VISION_RESULT_SUMMARY
USER_FEEDBACK_ADDRESSED
BUILD_RESULT
TEST_RESULT
IMPLEMENTED_THIS_ROUND
REMAINING_UI_DIFFERENCES
REMAINING_FUNCTIONAL_GAPS
ACTUAL_SCREENSHOT_BLOCKER
BLOCKERS
REGRESSION_RISKS
NEXT_RECOMMENDATION
WINUI3_CONFIRMED
WINDOWS_APP_SDK_CONFIRMED
NON_WINUI_FRAMEWORK_AVOIDED
WIN11_NATIVE_STYLE_PROGRESS
INVALID_XAML_MEMBERS_FIXED
WMC0011_REMAINING
DEBUG_ARM64_BUILD_RESULT
WINDOW_LAUNCH_RESULT
WINDOWS_HOST_VALIDATION_PENDING

END_PROMPT. Now execute Round 3 and return a structured report when complete.
