# Forgis Full Project Replica Pass for Kikaria Android

## 目标
对现有 Kikaria Android 项目 (`Kikaria-Android`) 做一次全量、系统性的复刻。目标是：
1. 保持原有 Android 工程目录结构。
2. 完整复刻源 iOS Kikaria 的 UI、交互、主题、字体、图标、按钮、卡片、气泡、布局和组件行为。
3. 保留所有核心功能：首页、复习/学习页面、预设/知识点管理、重要/已掌握列表、设置与个人页、导航和状态。
4. 不重写架构，不创建新 Material 样式，不增加无关页面或功能。
5. 严格只在 `Kikaria-Android` 下写入。
6. 保持源仓库只读，目标仓库其它文件不动。
7. 确保组件复用、主题统一、字体系统集中、图标映射完整。

## 执行策略
1. **先检查目标 Android 文件**，确认现有实现。
2. **再读取源 iOS 源仓库文件**，确认组件、布局、主题、字体、图标、交互逻辑。
3. **列出差异清单**，包含：
   - 布局偏差
   - 字体不一致
   - 图标不一致
   - 按钮/卡片/气泡尺寸差异
   - 交互行为差异
4. **逐步修复**：
   - 优先修主题、字体、图标、共享组件。
   - 再修屏幕布局、组件位置。
   - 再修交互/状态逻辑。
   - 所有变动都记录到 `git_diff`。
5. **不要提前结束**：
   - 确保所有主要屏幕和共享组件都完成对齐。
   - 对无法完成的部分明确标记 deferred 并说明原因。
6. **验证输出**：
   - 编译通过
   - 关键屏幕可运行
   - 所有修改仍在 `Kikaria-Android`
   - git_diff 可追踪
7. **迭代预算**：
   - 使用高迭代次数，充分利用放宽参数：
     - `max_iterations: 4500`
     - `max_tool_result_chars: 4500000`
     - `max_command_output_chars: 1800000`
     - `run_report_max_events: 9000`
     - `run_report_max_chars: 18000000`

## 核心要求
- UI、字体、图标、按钮、卡片、气泡、布局、交互行为与源 iOS 尽量一致。
- 所有共享组件必须集中管理，不能散落。
- 状态逻辑、每日目标、复习/已掌握、重要集锦、预设切换等必须保持原意。
- 不新增无关功能、不硬编码用户信息、不添加秘密/路径。
- 遇到不可实现或 Android 限制的差异，生成 TODO 注释。
- 不做 build/test 命令伪造。
- 输出报告包括：
  - 文件修改列表
  - 组件/主题/字体/图标修复情况
  - 差异未解决部分
  - deferred 标记及原因

## 注意
- 这是一次通宵级别全量复刻任务，不要再按小修小补模式结束。
- 不要访问 Forgis 目录外文件，不要修改源仓库，不要联网。
- 输出 final summary 要具体且 grounded。
